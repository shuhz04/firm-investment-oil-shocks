# Output

Generated model outputs are not included in this public repository because they are produced from proprietary firm-level data.

When the scripts are run with authorized access to the required datasets, this folder will contain:

- labeled regression tables;
- cumulative-effect summaries;
- session information for reproducibility.

The R analysis script writes these outputs to `output/tables/` and records the computing environment in `output/session_info.txt`.
